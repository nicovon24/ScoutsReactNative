---
phase: "05-clubes"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/queries/use-team-squad.ts
autonomous: true
requirements:
  - CLUB-03
  - CLUB-05

must_haves:
  truths:
    - "useTeamSquad(teamId) devuelve SquadPlayer[] desde la cache de React Query o hace fetch al endpoint"
    - "La query key ['team-squad', teamId] coincide exactamente con la usada en useLeaguePlayers"
    - "El hook no dispara fetches si teamId <= 0"
  artifacts:
    - path: "src/hooks/queries/use-team-squad.ts"
      provides: "Hook de datos de plantilla por equipo"
      exports: ["useTeamSquad"]
  key_links:
    - from: "src/hooks/queries/use-team-squad.ts"
      to: "src/lib/bzzoiro/endpoints.ts"
      via: "getTeamSquad(teamId)"
      pattern: "getTeamSquad"
    - from: "src/hooks/queries/use-team-squad.ts"
      to: "src/hooks/queries/use-league.ts"
      via: "queryKey: ['team-squad', teamId]"
      pattern: "team-squad"
---

<objective>
Crear el hook useTeamSquad para obtener la plantilla de un equipo. El hook debe usar la misma query key que useLeaguePlayers para garantizar cache sharing — cuando el usuario llega a la pantalla de detalle de un club después de haber visitado el home, el fetch ya estará resuelto en cache.

Purpose: Capa de datos para las pantallas de clubs/[id].tsx (plantilla y alineación). Sin este hook las pantallas Wave 2 y Wave 3 no tienen datos.
Output: src/hooks/queries/use-team-squad.ts exportando useTeamSquad.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

<interfaces>
<!-- Tipos relevantes extraídos de src/lib/bzzoiro/types.ts -->
```typescript
export interface SquadPlayer {
  id: number;
  name: string;
  short_name: string;
  position: 'G' | 'D' | 'M' | 'F' | '';
  jersey_number: number | null;
  nationality: string;
  date_of_birth: string | null;
}

export interface SquadResponse {
  team_id: number;
  count: number;
  players: SquadPlayer[];
}
```

<!-- Función de endpoint en src/lib/bzzoiro/endpoints.ts -->
```typescript
export async function getTeamSquad(teamId: number): Promise<SquadResponse>
// llama a: bzzoiroFetch<SquadResponse>(`/teams/${teamId}/squad/`)
```

<!-- Query key en uso en src/hooks/queries/use-league.ts (línea 39) -->
```typescript
queryKey: ['team-squad', entry.team_id],
// CRÍTICO: el nuevo hook DEBE usar exactamente esta misma key
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear hook useTeamSquad</name>
  <files>src/hooks/queries/use-team-squad.ts</files>
  <read_first>
    - src/hooks/queries/use-league.ts — verificar la query key exacta ['team-squad', teamId] en línea 39 antes de escribir
    - src/lib/bzzoiro/endpoints.ts — confirmar firma de getTeamSquad
    - src/lib/bzzoiro/types.ts — importar SquadResponse
  </read_first>
  <action>
    Crear el archivo src/hooks/queries/use-team-squad.ts con el hook exportado useTeamSquad(teamId: number).

    Implementación exacta requerida:
    - import { useQuery } from '@tanstack/react-query'
    - import { getTeamSquad } from '@/lib/bzzoiro/endpoints'
    - import type { SquadResponse } from '@/lib/bzzoiro/types'
    - queryKey: ['team-squad', teamId] — DEBE ser idéntica a la usada en use-league.ts línea 39
    - queryFn: () => getTeamSquad(teamId)
    - staleTime: 1000 * 60 * 60 * 24 (24h, igual que todas las squad queries)
    - enabled: teamId > 0

    El tipo de retorno inferido de useQuery es UseQueryResult<SquadResponse>. No añadir anotación manual de tipo de retorno — dejar que TypeScript lo infiera.

    Sin comentarios narrativos. Sin StyleSheet. Un export nombrado: useTeamSquad.
  </action>
  <verify>
    Verificación manual: el archivo existe en src/hooks/queries/use-team-squad.ts, exporta useTeamSquad, no tiene errores de TypeScript (npx tsc --noEmit desde la raíz del proyecto).
  </verify>
  <acceptance_criteria>
    - src/hooks/queries/use-team-squad.ts existe y exporta useTeamSquad
    - La query key es literalmente ['team-squad', teamId] — sin variantes
    - staleTime es 24 * 60 * 60 * 1000
    - enabled: teamId > 0 presente
    - No hay uso de `any` ni `ts-ignore`
    - npx tsc --noEmit no reporta errores en este archivo
  </acceptance_criteria>
  <done>Hook creado, tipado correcto, query key alineada con use-league.ts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descripción |
|----------|-------------|
| app → BSD REST API | Fetch autenticado via bzzoiroFetch — token ya validado en client.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | EXPO_PUBLIC_BZZOIRO_API_KEY en bundle | accept | Token ya expuesto en fases previas; riesgo conocido, documentado en AGENTS.md |
| T-05-SC | Tampering | npm installs | accept | No hay nuevos paquetes en esta fase |
</threat_model>

<verification>
- npx tsc --noEmit desde la raíz no reporta errores en src/hooks/queries/use-team-squad.ts
- Grep confirma que la query key en use-team-squad.ts coincide con use-league.ts: grep "team-squad" src/hooks/queries/use-team-squad.ts
</verification>

<success_criteria>
useTeamSquad(42) devuelve datos de cache si useLeaguePlayers ya cargó (hot path), o dispara fetch a /teams/42/squad/ si es la primera visita (cold path). teamId <= 0 no dispara ningún fetch.
</success_criteria>

<output>
Crear .planning/phases/05-clubes/05-01-SUMMARY.md cuando termine
</output>
