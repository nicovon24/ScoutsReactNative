---
phase: "05-clubes"
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/components/scout/pitch-formation.tsx
  - src/app/clubs/[id].tsx
autonomous: true
requirements:
  - CLUB-03
  - CLUB-04
  - CLUB-05

must_haves:
  truths:
    - "La pantalla /clubs/[id] muestra el nombre y logo del club en el header"
    - "La pestaña Plantilla agrupa los jugadores por posicion (Porteros / Defensores / Centrocampistas / Delanteros)"
    - "Cada fila de jugador muestra foto miniatura, numero, nombre y nacionalidad"
    - "La pestaña Partidos muestra un placeholder con icono y mensaje 'Proxinamente'"
    - "La pestaña Alineacion muestra el campo SVG (PitchFormation) con los jugadores posicionados en formacion 4-3-3"
    - "El boton de volver navega a /clubs si no hay historial previo"
    - "generateStaticParams exporta los 20 team IDs para el build web estatico"
  artifacts:
    - path: "src/components/scout/pitch-formation.tsx"
      provides: "SVG de campo con jugadores posicionados en formacion estimada 4-3-3"
      exports: ["PitchFormation"]
    - path: "src/app/clubs/[id].tsx"
      provides: "Pantalla de detalle de club con 3 pestanas"
      exports: ["default (screen component)", "generateStaticParams"]
  key_links:
    - from: "src/app/clubs/[id].tsx"
      to: "useTeamSquad"
      via: "import from @/hooks/queries/use-team-squad"
      pattern: "useTeamSquad"
    - from: "src/app/clubs/[id].tsx"
      to: "useLeagueStandings"
      via: "para obtener team_name a partir del team_id de la URL"
      pattern: "useLeagueStandings"
    - from: "src/app/clubs/[id].tsx"
      to: "src/components/scout/pitch-formation.tsx"
      via: "tab === 'alineacion' && <PitchFormation players={...} />"
      pattern: "PitchFormation"
    - from: "src/components/scout/pitch-formation.tsx"
      to: "react-native-svg"
      via: "Svg, Rect, Circle, Text as SvgText, Line"
      pattern: "react-native-svg"
---

<objective>
Construir la pantalla de detalle de club (/clubs/[id]) con las tres pestanas: Plantilla (jugadores agrupados por posicion), Partidos (placeholder), y Alineacion (campo SVG con formacion estimada 4-3-3). Tambien crea el componente PitchFormation que renderiza el SVG del campo.

Purpose: CLUB-03, CLUB-04, CLUB-05 — pantalla completa de club con toda la informacion disponible de la API mas la visualizacion SVG del campo.
Output: src/components/scout/pitch-formation.tsx y src/app/clubs/[id].tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-clubes/05-01-SUMMARY.md
@.planning/phases/05-clubes/05-02-SUMMARY.md

<interfaces>
<!-- Tipos de src/lib/bzzoiro/types.ts -->
```typescript
export interface SquadPlayer {
  id: number;
  name: string;
  short_name: string;
  position: 'G' | 'D' | 'M' | 'F' | '';
  jersey_number: number | null;
  nationality: string;
  date_of_birth: string | null;
}

export interface SquadResponse {
  team_id: number;
  count: number;
  players: SquadPlayer[];
}

export interface StandingEntry {
  position: number;
  team_id: number;
  team_name: string;
  pts: number;
  // ... otros campos
}
```

<!-- Hook recien creado en 05-01 -->
```typescript
// src/hooks/queries/use-team-squad.ts
export function useTeamSquad(teamId: number): UseQueryResult<SquadResponse>
// queryKey: ['team-squad', teamId], staleTime: 24h, enabled: teamId > 0
```

<!-- Hook existente en src/hooks/queries/use-league.ts -->
```typescript
export function useLeagueStandings(leagueId: number): UseQueryResult<StandingsResponse>
```

<!-- Constante en src/lib/bzzoiro/endpoints.ts -->
```typescript
export const PREMIER_LEAGUE_ID: LeagueId = 1;
export async function getStandings(leagueId?: number): Promise<StandingsResponse>
```

<!-- Patron de design tokens de src/app/player/[id].tsx -->
```typescript
const C = {
  bg: '#0F0F0F', surface: '#161616', card: '#1C1C1C',
  border: '#2C2C2C', borderH: '#3C3C3C',
  primary: '#F2F2F2', secondary: '#B8B8B8', muted: '#717171',
  green: '#64ffda',
};
```

<!-- react-native-svg ya instalado (v15.15.4) -->
```typescript
import Svg, { Rect, Circle, Text as SvgText, Line } from 'react-native-svg';
// ya usado en SkillRadar, DonutCircle, RatingEvolution, CompareRadar
```

<!-- generateStaticParams patron de src/app/player/[id].tsx lineas 29-32 -->
```typescript
export async function generateStaticParams(): Promise<Record<string, string>[]> {
  const ids = await getLeaguePlayerIds();  // equivalente para teams: getStandings().standings.map(t => t.team_id)
  return ids.map((id) => ({ id: String(id) }));
}
```

<!-- URL de foto de jugador (verificada) -->
```typescript
const photoUrl = `https://sports.bzzoiro.com/img/player/${player.id}/`;
```

<!-- PitchPositionPicker existente en src/components/scout/pitch-position-picker.tsx -->
// NOTA: este archivo ES UN FILTRO INTERACTIVO para la pantalla de jugadores.
// NO reutilizar para CLUB-05 — el nuevo componente PitchFormation es diferente:
// muestra jugadores reales del squad con jersey_number, no botones de filtro.
// PitchFormation usa react-native-svg (Svg/Circle/SvgText) en vez de Views absolutas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear componente PitchFormation</name>
  <files>src/components/scout/pitch-formation.tsx</files>
  <read_first>
    - src/lib/bzzoiro/types.ts — interfaz SquadPlayer (position, jersey_number, short_name)
    - src/components/scout/pitch-position-picker.tsx — leer para entender la diferencia (es filtro, no display); NO copiar su estructura
    - src/components/scout/skill-radar.tsx — patron de uso de react-native-svg en el proyecto
  </read_first>
  <action>
    Crear src/components/scout/pitch-formation.tsx. Este componente recibe un array de SquadPlayer y renderiza un SVG de campo de futbol con los jugadores de la formacion estimada 4-3-3 posicionados en el campo.

    Props: { players: SquadPlayer[] }

    Logica de seleccion de titulares (pure client-side dentro del componente):
    - Agrupar players por position usando filter
    - Tomar el primero de cada grupo segun la formacion: 1 G, 4 D (primeros 4), 3 M (primeros 3), 3 F (primeros 3)
    - Si un grupo no tiene suficientes jugadores, mostrar los disponibles sin error
    - Tipo interno: type StartingPlayer = SquadPlayer & { coord: { x: number; y: number } }

    Coordenadas de formacion (porcentajes del viewBox, origin arriba-izquierda, portero abajo):
    ```
    const FORMATION: Record<string, Array<{ x: number; y: number }>> = {
      G: [{ x: 0.50, y: 0.88 }],
      D: [{ x: 0.18, y: 0.70 }, { x: 0.38, y: 0.70 }, { x: 0.62, y: 0.70 }, { x: 0.82, y: 0.70 }],
      M: [{ x: 0.25, y: 0.50 }, { x: 0.50, y: 0.50 }, { x: 0.75, y: 0.50 }],
      F: [{ x: 0.22, y: 0.26 }, { x: 0.50, y: 0.22 }, { x: 0.78, y: 0.26 }],
    };
    ```

    SVG (react-native-svg):
    - Dimensiones: width="100%" con viewBox="0 0 300 440"
    - Fondo del campo: Rect fill="#0D2B0D" rx={8} (verde oscuro)
    - Lineas del campo (todas con stroke="rgba(255,255,255,0.2)" strokeWidth={1}):
      - Linea de medio campo: Line x1={0} y1={220} x2={300} y2={220}
      - Circulo central: Circle cx={150} cy={220} r={36} fill="none"
      - Borde campo: Rect x={12} y={12} width={276} height={416} fill="none"
      - Area grande superior: Rect x={78} y={12} width={144} height={72} fill="none"
      - Area grande inferior: Rect x={78} y={356} width={144} height={72} fill="none"
      - Area chica superior: Rect x={108} y={12} width={84} height={30} fill="none"
      - Area chica inferior: Rect x={108} y={398} width={84} height={30} fill="none"
    - Por cada jugador titular: Circle (dot) + SvgText encima con el jersey_number
      - Circle: cx={x * 300} cy={y * 440} r={14} fill="#64ffda" (acento turquesa)
      - SvgText del numero: textAnchor="middle" x={cx} y={cy + 4} fontSize={10} fontWeight="bold" fill="#000"
      - SvgText del nombre debajo: textAnchor="middle" x={cx} y={cy + 22} fontSize={8} fill="rgba(255,255,255,0.8)" — usar jersey_number ?? short_name como label del dot, y short_name cortado a 8 chars como nombre debajo

    Texto "Formacion estimada" en la parte superior del SVG:
    - SvgText x={150} y={8} textAnchor="middle" fontSize={7} fill="rgba(255,255,255,0.4)" — texto: "Formacion estimada 4-3-3"

    Imports:
    - import Svg, { Rect, Circle, Text as SvgText, Line } from 'react-native-svg'
    - import type { SquadPlayer } from '@/lib/bzzoiro/types'

    Sin StyleSheet.create. Export nombrado: PitchFormation.
  </action>
  <verify>
    npm run web y navegar a /clubs/[id] pestana Alineacion: el campo SVG se renderiza con 11 puntos turquesa. No hay errores de TypeScript (npx tsc --noEmit).
  </verify>
  <acceptance_criteria>
    - src/components/scout/pitch-formation.tsx existe y exporta PitchFormation
    - Acepta prop players: SquadPlayer[]
    - Usa react-native-svg (Svg, Rect, Circle, SvgText, Line) — no react-native Views para las lineas del campo
    - Muestra maximo 11 jugadores (1+4+3+3)
    - Cada jugador se muestra como Circle #64ffda con jersey_number en negro
    - Sin crashes si un grupo de posicion tiene menos jugadores de los esperados
    - Sin uso de `any`
    - npx tsc --noEmit sin errores
  </acceptance_criteria>
  <done>PitchFormation renderiza campo SVG con formacion 4-3-3 usando jugadores reales del squad</done>
</task>

<task type="auto">
  <name>Task 2: Crear pantalla clubs/[id].tsx</name>
  <files>src/app/clubs/[id].tsx</files>
  <read_first>
    - src/app/player/[id].tsx — solo para entender el patron de generateStaticParams y useLocalSearchParams; NO copiar design tokens C ni inline styles
    - src/app/index.tsx — patron de NativeWind className en screens (este archivo usa NativeWind extensamente)
    - src/lib/static-params.ts — entender getLeaguePlayerIds para replicar el patron con getStandings para teams
    - src/hooks/queries/use-team-squad.ts — recien creado en 05-01
    - src/hooks/queries/use-league.ts — useLeagueStandings para obtener team_name desde standings
  </read_first>
  <action>
    Crear src/app/clubs/[id].tsx como pantalla de detalle de club. Usar NativeWind (className) para todos los estilos. Sin const C. Sin StyleSheet.create. Sin ThemedText/ThemedView. Solo inline style={{ }} para colores no disponibles en Tailwind como '#64ffda'.

    generateStaticParams (export async function, requerido para build web):
    - Llamar a getStandings() directamente (import de @/lib/bzzoiro/endpoints)
    - Retornar standings.standings.map(t => ({ id: String(t.team_id) }))

    Estado:
    - const { id } = useLocalSearchParams<{ id: string }>()
    - const teamId = Number(id)
    - const [tab, setTab] = useState<'plantilla' | 'partidos' | 'alineacion'>('plantilla')
    - const { data: standingsData } = useLeagueStandings(PREMIER_LEAGUE_ID)
    - const { data: squadData, isLoading: squadLoading } = useTeamSquad(teamId)
    - const teamEntry = standingsData?.standings.find(t => t.team_id === teamId)

    Header (NativeWind):
    - View raíz: className="flex-1 bg-black"
    - Boton volver: Pressable className="p-4", ChevronLeft color="#f4f4f5" size={22}; onPress: router.canGoBack() ? router.back() : router.replace('/clubs')
    - Logo del equipo: Image (expo-image) className="w-14 h-14", contentFit="contain", source={{ uri: `https://sports.bzzoiro.com/img/team/${teamId}/` }}
    - Nombre del equipo: Text className="text-white text-[22px] font-black"
    - Sublinea: Text className="text-zinc-500 text-sm" — `#${teamEntry.position} · ${teamEntry.pts} pts`

    Barra de tabs (NativeWind):
    - View className="flex-row border-b border-zinc-800 mt-4"
    - Cada Pressable tab: className="flex-1 py-3 items-center"
    - Tab activo: style={{ borderBottomWidth: 2, borderBottomColor: '#64ffda' }} + Text style={{ color: '#64ffda' }} className="text-sm font-bold"
    - Tab inactivo: Text className="text-zinc-500 text-sm font-bold"
    - Labels: { plantilla: 'Plantilla', partidos: 'Partidos', alineacion: 'Alineacion' }

    Contenido por tab:

    TAB 'plantilla' (CLUB-03):
    - Si squadLoading: ActivityIndicator color="#64ffda" (prop, no className)
    - Agrupar players de squadData.players por position usando la funcion groupByPosition definida en el archivo
    - POSITION_LABELS: { G: 'Porteros', D: 'Defensores', M: 'Centrocampistas', F: 'Delanteros', '': 'Sin posicion' }
    - POSITION_ORDER: ['G', 'D', 'M', 'F', '']
    - Por cada grupo: Text className="text-zinc-500 text-xs uppercase tracking-widest font-bold mb-2 px-5 pt-4", luego filas de jugadores
    - Fila de jugador (NativeWind): View className="flex-row items-center gap-2.5 py-2 px-5 border-b border-zinc-800"
      - Image 32x32: className="w-8 h-8 rounded-full bg-zinc-800", contentFit="cover", source={{ uri: `https://sports.bzzoiro.com/img/player/${p.id}/` }}
      - Numero de camiseta: Text className="text-zinc-500 text-xs w-6 text-center"
      - Nombre: Text className="text-white text-[13px] font-bold flex-1"
      - Nacionalidad: Text className="text-zinc-500 text-[11px]"

    TAB 'partidos' (CLUB-04):
    - View className="items-center justify-center p-8 gap-3"
    - Clock icon (lucide-react-native, size=32, color="#71717a")
    - Text className="text-white text-base font-black">"Proximamente"
    - Text className="text-zinc-500 text-[13px] text-center leading-5">"Los partidos de este club no estan disponibles en esta version."

    TAB 'alineacion' (CLUB-05):
    - Si squadLoading: ActivityIndicator color="#64ffda"
    - <PitchFormation players={squadData?.players ?? []} />
    - Text className="text-zinc-500 text-xs text-center mt-2 px-5">"Formacion estimada basada en la plantilla del club."

    Imports requeridos:
    - { useState } from 'react'
    - { View, Text, ScrollView, ActivityIndicator, Pressable } from 'react-native'
    - { Image } from 'expo-image'
    - { useLocalSearchParams, useRouter } from 'expo-router'
    - { ChevronLeft, Clock } from 'lucide-react-native'
    - { useLeagueStandings } from '@/hooks/queries/use-league'
    - { useTeamSquad } from '@/hooks/queries/use-team-squad'
    - { PREMIER_LEAGUE_ID, getStandings } from '@/lib/bzzoiro/endpoints'
    - { PitchFormation } from '@/components/scout/pitch-formation'
    - type { SquadPlayer } from '@/lib/bzzoiro/types'

    Sin StyleSheet.create. Sin const C. Sin ThemedText/ThemedView. Export default del componente.
  </action>
  <verify>
    npm run web, navegar a /clubs, tocar un equipo: se abre el detalle con logo y nombre del equipo. Las tres pestanas son presionables. Plantilla muestra grupos de posicion con fotos. Partidos muestra el placeholder. Alineacion muestra el campo SVG. npx tsc --noEmit sin errores.
  </verify>
  <acceptance_criteria>
    - src/app/clubs/[id].tsx existe con export default y export async function generateStaticParams
    - generateStaticParams retorna array de { id: string } con los 20 team IDs
    - Usa useTeamSquad(teamId) del hook de 05-01 — no llama a getTeamSquad directamente
    - Usa useLeagueStandings para obtener team_name (no hardcodea nombres)
    - Tab 'plantilla': jugadores agrupados por G/D/M/F con SectionTitle por grupo
    - Tab 'partidos': placeholder con icono y mensaje en espanol
    - Tab 'alineacion': <PitchFormation> recibe players del squad
    - Boton volver llama router.replace('/clubs') cuando canGoBack() es false
    - Sin StyleSheet.create
    - Sin uso de `any`
    - npx tsc --noEmit sin errores en este archivo
  </acceptance_criteria>
  <done>Pantalla /clubs/[id] operativa con las tres pestanas: plantilla agrupada, placeholder de partidos, y campo SVG con formacion estimada</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descripcion |
|----------|-------------|
| URL param id → teamId | useLocalSearchParams provee string — se convierte con Number(); si NaN, useTeamSquad no dispara (enabled: teamId > 0) |
| app → BSD REST API | Fetch autenticado via bzzoiroFetch — token ya validado en client.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | URL param id en clubs/[id].tsx | mitigate | Number(id) con enabled: teamId > 0 en useTeamSquad previene fetch con NaN/0 |
| T-05-05 | Information Disclosure | EXPO_PUBLIC_BZZOIRO_API_KEY en bundle | accept | Token ya expuesto en fases previas; riesgo documentado |
| T-05-SC | Tampering | npm installs | accept | No hay nuevos paquetes en esta fase |
</threat_model>

<verification>
- npx tsc --noEmit sin errores en ambos archivos nuevos
- npm run web: /clubs/[id] para cualquier team ID carga correctamente
- Pestana Plantilla muestra grupos Porteros / Defensores / Centrocampistas / Delanteros
- Pestana Partidos muestra placeholder con mensaje en espanol
- Pestana Alineacion muestra campo SVG verde con puntos turquesa y numeros de camiseta
- npm run web build: generateStaticParams no lanza errores (todos los IDs son numericos validos)
</verification>

<success_criteria>
La pantalla /clubs/[id] completa: header con logo y nombre del club, 3 pestanas funcionales. Plantilla agrupa jugadores por posicion con fotos y numero de camiseta. Partidos es un placeholder elegante. Alineacion muestra el campo SVG con la formacion 4-3-3 estimada a partir de la plantilla real.
</success_criteria>

<output>
Crear .planning/phases/05-clubes/05-03-SUMMARY.md cuando termine
</output>
