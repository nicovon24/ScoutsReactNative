---
phase: "05-clubes"
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/scout/club-card.tsx
  - src/app/clubs/index.tsx
autonomous: true
requirements:
  - CLUB-01
  - CLUB-02

must_haves:
  truths:
    - "Navegar a /clubs desde el sidebar muestra una grilla de los 20 equipos de la Premier League"
    - "La barra de búsqueda filtra los equipos por nombre en tiempo real (client-side)"
    - "Tocar una card navega a /clubs/[id]"
    - "Cada card muestra el logo del equipo, nombre, posición en la tabla y puntos"
    - "Mientras cargan los datos aparece un skeleton o indicador de carga"
  artifacts:
    - path: "src/components/scout/club-card.tsx"
      provides: "Card de equipo para la grilla"
      exports: ["ClubCard"]
    - path: "src/app/clubs/index.tsx"
      provides: "Pantalla lista de clubes con búsqueda y FlatList"
      exports: ["default (screen component)"]
  key_links:
    - from: "src/app/clubs/index.tsx"
      to: "useLeagueStandings"
      via: "import from @/hooks/queries/use-league"
      pattern: "useLeagueStandings"
    - from: "src/app/clubs/index.tsx"
      to: "src/components/scout/club-card.tsx"
      via: "renderItem de FlatList"
      pattern: "ClubCard"
    - from: "src/components/scout/club-card.tsx"
      to: "/clubs/[id]"
      via: "router.push"
      pattern: "router.push.*clubs"
---

<objective>
Construir la pantalla de lista de clubes (/clubs) con su componente ClubCard. La pantalla usa useLeagueStandings (datos ya en cache desde el home) para mostrar los 20 equipos en una FlatList de 2 columnas con barra de búsqueda.

Purpose: CLUB-02 — el usuario puede ver y buscar los 20 clubes de la Premier League. CLUB-01 ya está completo (sidebar tiene el item Clubes).
Output: src/components/scout/club-card.tsx y src/app/clubs/index.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

<interfaces>
<!-- Tipos de src/lib/bzzoiro/types.ts -->
```typescript
export interface StandingEntry {
  position: number;
  team_id: number;
  team_name: string;
  played: number;
  won: number;
  drawn: number;
  lost: number;
  gf: number;
  ga: number;
  gd: number;
  pts: number;
  form: string;
  live: boolean;
  // más campos xG disponibles pero no necesarios en esta pantalla
}

export interface StandingsResponse {
  league_id: number;
  season: Season;
  standings: StandingEntry[];
}
```

<!-- Hook disponible en src/hooks/queries/use-league.ts -->
```typescript
export function useLeagueStandings(leagueId: number): UseQueryResult<StandingsResponse>
// queryKey: ['league-standings', leagueId]
// staleTime: 24h
```

<!-- Constante disponible en src/lib/bzzoiro/endpoints.ts -->
```typescript
export const PREMIER_LEAGUE_ID: LeagueId = 1;
```

<!-- Patrón de design tokens de src/app/player/[id].tsx (líneas 36-48) -->
```typescript
const C = {
  bg:       '#0F0F0F',
  surface:  '#161616',
  card:     '#1C1C1C',
  border:   '#2C2C2C',
  primary:  '#F2F2F2',
  secondary:'#B8B8B8',
  muted:    '#717171',
  green:    '#64ffda',   // acento de esta sección
};
```

<!-- Logo URL pattern (verificado desde research) -->
```typescript
const logoUrl = `https://sports.bzzoiro.com/img/team/${team_id}/`;
// usar con expo-image: <Image source={{ uri: logoUrl }} contentFit="contain" />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear componente ClubCard</name>
  <files>src/components/scout/club-card.tsx</files>
  <read_first>
    - src/lib/bzzoiro/types.ts — interfaz StandingEntry completa
    - src/app/player/[id].tsx — patrón de design tokens C, uso de expo-image, inline styles (no StyleSheet)
    - src/app/compare.tsx — patrón de Pressable card con inline styles
  </read_first>
  <action>
    Crear src/components/scout/club-card.tsx con el componente ClubCard.

    Props: { entry: StandingEntry; onPress: () => void }

    Estructura visual (todo NativeWind, sin const C, sin StyleSheet.create):
    - Pressable exterior: className="bg-zinc-900 rounded-2xl border border-zinc-800 p-4 items-center gap-2"
    - Image (expo-image) del logo: source={{ uri: `https://sports.bzzoiro.com/img/team/${entry.team_id}/` }}, className="w-[52px] h-[52px]", contentFit="contain"
    - Nombre del equipo: Text con className="text-white text-[13px] font-black text-center", numberOfLines={2}
    - Fila inferior: Text con className="text-zinc-500 text-[11px] text-center" — formato: `#${entry.position}  ·  ${entry.pts} pts`

    Imports requeridos:
    - import { Pressable, Text } from 'react-native'
    - import { Image } from 'expo-image'
    - import type { StandingEntry } from '@/lib/bzzoiro/types'

    Sin StyleSheet.create. Sin const C. Sin ThemedText/ThemedView. Sin emojis. Sin comentarios narrativos.
  </action>
  <verify>
    El archivo existe en src/components/scout/club-card.tsx y exporta ClubCard. npx tsc --noEmit no reporta errores.
  </verify>
  <acceptance_criteria>
    - src/components/scout/club-card.tsx existe y exporta ClubCard como named export
    - Props tipadas con StandingEntry de @/lib/bzzoiro/types
    - Usa expo-image (Image de 'expo-image'), no react-native Image
    - Logo URL: `https://sports.bzzoiro.com/img/team/${entry.team_id}/`
    - contentFit="contain" en la Image
    - Sin StyleSheet.create
    - Sin uso de `any`
    - Muestra team_name, position, pts
  </acceptance_criteria>
  <done>ClubCard renderiza logo, nombre del equipo, posición y puntos con estilos inline correctos</done>
</task>

<task type="auto">
  <name>Task 2: Crear pantalla clubs/index.tsx</name>
  <files>src/app/clubs/index.tsx</files>
  <read_first>
    - src/app/index.tsx — patrón de FlatList con búsqueda y estado de carga
    - src/hooks/queries/use-league.ts — uso de useLeagueStandings, tipo StandingsResponse
    - src/components/scout/club-card.tsx — recién creado, revisar props
    - src/app/player/[id].tsx — patrón de Screen root con SafeAreaView o ScrollView raíz
  </read_first>
  <action>
    Crear src/app/clubs/index.tsx como pantalla de lista de clubes.

    Estado local:
    - const [search, setSearch] = useState('')

    Datos:
    - const { data, isLoading } = useLeagueStandings(PREMIER_LEAGUE_ID)
    - const teams = data?.standings ?? []
    - Filtrado client-side: search.trim() ? teams.filter(t => t.team_name.toLowerCase().includes(search.toLowerCase())) : teams

    Layout raíz (todo NativeWind, sin const C, sin StyleSheet.create):
    - View raíz: className="flex-1 bg-black"

    Header:
    - Text "Clubes": className="text-white text-2xl font-black px-5 pt-4"
    - Text subtítulo "20 equipos": className="text-zinc-500 text-sm px-5 mb-3"

    Barra de búsqueda:
    - TextInput: className="bg-zinc-900 rounded-xl border border-zinc-800 p-3 mx-5 mb-3 text-white text-sm"
    - placeholder="Buscar club...", placeholderTextColor="#71717a" (hex directo, no es prop de className)

    FlatList:
    - data={filtered} (la variable filtrada)
    - keyExtractor={(item) => String(item.team_id)}
    - numColumns={2}
    - columnWrapperStyle={{ gap: 12, paddingHorizontal: 20 }} (prop de FlatList, no admite className)
    - ItemSeparatorComponent: View con className="h-3"
    - contentContainerStyle={{ paddingBottom: 32 }} (prop de FlatList)
    - renderItem: ({ item }) => <ClubCard entry={item} onPress={() => router.push(`/clubs/${item.team_id}`)} />
    - ListEmptyComponent: si isLoading, ActivityIndicator con color="#64ffda" (prop, no className); si no, Text con className="text-zinc-500 text-center"

    Imports requeridos:
    - { useState } from 'react'
    - { View, Text, FlatList, TextInput, ActivityIndicator } from 'react-native'
    - { useRouter } from 'expo-router'
    - { useLeagueStandings } from '@/hooks/queries/use-league'
    - { PREMIER_LEAGUE_ID } from '@/lib/bzzoiro/endpoints'
    - { ClubCard } from '@/components/scout/club-card'

    Sin StyleSheet.create. Sin ThemedText/ThemedView. Export default del componente de screen.
  </action>
  <verify>
    npm run web abre /clubs sin errores de consola. Se ven 20 cards de equipos. El buscador filtra al escribir.
  </verify>
  <acceptance_criteria>
    - src/app/clubs/index.tsx existe con export default
    - Usa useLeagueStandings(PREMIER_LEAGUE_ID) — no hace fetch propio al endpoint de equipos
    - FlatList con numColumns={2} (no ScrollView + map)
    - Filtrado client-side por team_name en minúsculas
    - ActivityIndicator visible mientras isLoading=true
    - onPress de cada ClubCard llama router.push con `/clubs/${item.team_id}`
    - Sin StyleSheet.create
    - npx tsc --noEmit sin errores en este archivo
  </acceptance_criteria>
  <done>Pantalla /clubs muestra 20 clubes en grilla 2 columnas con buscador funcional</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descripción |
|----------|-------------|
| TextInput search → filter | Input local, filtrado en cliente — sin datos sensibles ni persistencia |
| app → BSD CDN | Logos via URL pública — sin auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | EXPO_PUBLIC_BZZOIRO_API_KEY en bundle | accept | Mismo token ya expuesto en fases previas |
| T-05-03 | Tampering | TextInput search query | accept | Filtrado solo client-side, no se envía a ningún servidor |
| T-05-SC | Tampering | npm installs | accept | No hay nuevos paquetes en esta fase |
</threat_model>

<verification>
- npx tsc --noEmit no reporta errores en los dos archivos nuevos
- npm run web: navegar a /clubs muestra grilla de 20 equipos
- Escribir en el buscador filtra equipos en tiempo real
- Tocar un equipo navega a /clubs/[id] (la pantalla puede no existir aún — es normal en Wave 2)
</verification>

<success_criteria>
/clubs carga y muestra los 20 equipos de la Premier League en una grilla 2 columnas. El buscador filtra por nombre. Cada card muestra logo, nombre, posición y puntos. Navegar desde el sidebar con Shield icon funciona.
</success_criteria>

<output>
Crear .planning/phases/05-clubes/05-02-SUMMARY.md cuando termine
</output>
